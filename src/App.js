import {SafeAreaView, View, Text} from 'react-native';
import React from 'react';
import Todos from './Todos';

const App = () => {
  return (
    <SafeAreaView>
      <Todos />
    </SafeAreaView>
  );
};

export default App;
